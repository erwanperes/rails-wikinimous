<h1>Articles</h1>
<ul>
  <% @articles.each do |article| %>
    <div>
      <h3><%= link_to article.title, article %></h3>
      <p><%= truncate(article.content, length: 50, omission: '...') %></p>
    </div>
  <% end %>
</ul>

